name: uni_assistant
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# TODO also change lib\services\GithubServices.dart > currentVersion
version: 1.8.1+12

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.16.1 # DateFormat
  path_provider: ^1.6.11 # saving
  flutter_local_notifications: ^3.0.1+6 # notifications
  flutter_native_timezone: ^1.0.4
  github: ^7.0.4
  url_launcher: ^5.7.10
  fluttertoast: ^7.1.6
  shared_preferences: ^0.5.12+4 # Settings

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true

