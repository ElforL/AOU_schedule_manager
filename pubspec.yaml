name: uni_assistant
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.10.2+18

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0 # DateFormat
  path_provider: ^2.0.2 # saving
  flutter_local_notifications: ^6.0.0 # notifications
  flutter_native_timezone: ^2.0.0
  github: ^7.0.4
  url_launcher: ^6.0.6
  fluttertoast: ^7.1.6
  shared_preferences: ^2.0.6 # Settings
  package_info: ^0.4.3+4
  flutter_markdown: ^0.5.2
  xml: ^5.1.1

  cupertino_icons: ^0.1.3
  webview_flutter: ^2.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true

  assets:
    - assets/

